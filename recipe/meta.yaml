{% set name = "safety" %}
{% set version = "1.10.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 2ebc71b44666588d7898905d86d575933fcd5fa3c92d301ed12482602b1e928a

build:
  number: 0
  entry_points:
    - safety=safety.cli:cli
  script: {{ PYTHON }} -m pip install . --no-deps -vv
  noarch: python

requirements:
  host:
    - pip
    - python >=3.5
  run:
    - click >=6.0
    - dparse >=0.5.1
    - packaging
    - python >=3.5
    - requests
    - setuptools

test:
  source_files:
    - tests
  requires:
    - pip
    - pytest
  imports:
    - safety
  commands:
    - pip check
    - safety --help
    - pytest

about:
  home: https://pyup.io/safety
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Safety checks your installed dependencies for known security vulnerabilities.
  doc_url: https://pyup.io/docs
  dev_url: https://github.com/pyupio/safety

extra:
  recipe-maintainers:
    - jkornblum
    - bollwyvl
